# HALTON: Event Ontology Completion with Hierarchical Structure Evolution Networks

**train**
```bash
python main.py --dataset=ace --seed=1 --cuda
```

**test**
```bash
python test.py --dataset=ace --seed=1 --cuda
```